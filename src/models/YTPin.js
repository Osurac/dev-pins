export default class YTPin {
    constructor(id, url, user_id, fav,thumbnail, title,channelTitle) {
        this.id = id;
        this.url = url;
        this.user_id = user_id;
        this.fav = fav;
        this.thumbnail = thumbnail;
        this.title = title;
        this.channelTitle = channelTitle;
    }
}