import React from 'react';
import '../App.css';
import { Layout } from '../layout';

const PODLinks = () => (
    <Layout className="PODLinks">
        <p>
        PODLinks
        </p>
    </Layout>
);

export default PODLinks;